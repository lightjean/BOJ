#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    //int carry = 0, sum = 0;
    //string a, b;
    //cin >> a >> b;
    return 0;
}