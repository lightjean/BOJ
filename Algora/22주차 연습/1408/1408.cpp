#include <iostream>
#include <string.h>
using namespace std;

int main()
{
    int h1, h2, m1, m2, s1, s2;
    char now[8], start[8];

    cin >> now >> start;

    char *ptr1 = strtok(now, ":");
    char *ptr2 = strtok(start, ":");
}